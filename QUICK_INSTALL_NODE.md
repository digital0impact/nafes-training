# 🚀 تثبيت Node.js - دليل سريع

## المشكلة الحالية

```
npm : The term 'npm' is not recognized
```

هذا يعني أن **Node.js غير مثبت** أو **غير موجود في PATH**.

---

## ✅ الحل: تثبيت Node.js (5 دقائق)

### الخطوة 1: تحميل Node.js

1. افتحي المتصفح
2. اذهبي إلى: **https://nodejs.org/**
3. اضغطي على زر **LTS** (النسخة المستقرة - موصى بها)
4. سيبدأ التحميل تلقائياً

### الخطوة 2: تثبيت Node.js

1. شغّلي الملف الذي تم تحميله (مثل `node-v20.x.x-x64.msi`)
2. اتبعي معالج التثبيت:
   - اضغطي **Next** في جميع الخطوات
   - **⚠️ مهم جداً:** تأكدي من اختيار **"Add to PATH"** أو **"Automatically install the necessary tools"**
3. اضغطي **Install** وانتظري حتى يكتمل
4. اضغطي **Finish**

### الخطوة 3: إعادة فتح PowerShell

**⚠️ مهم جداً:**
1. **أغلقي جميع نوافذ PowerShell** (مهم!)
2. **افتحي PowerShell جديد**
3. جربي:

```powershell
node --version
npm --version
```

**إذا ظهرت الأرقام، فالتثبيت نجح! ✅**

---

## 🎯 بعد التثبيت: تشغيل التطبيق

بعد التأكد من أن npm يعمل:

```powershell
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"
npm install
npm run dev
```

---

## ❓ حل المشاكل

### المشكلة: npm لا يزال غير معروف بعد التثبيت

**الحل:**
1. تأكدي من **إغلاق وإعادة فتح PowerShell**
2. إذا لم يعمل، أعدي تثبيت Node.js واختاري **"Repair"** أو **"Reinstall"**
3. تأكدي من اختيار **"Add to PATH"** أثناء إعادة التثبيت

### المشكلة: "Access Denied"

**الحل:**
- شغّلي المثبت **كمسؤول** (Right-click → Run as administrator)

---

## 📋 استخدام السكريبت

شغّلي السكريبت المرفق للتحقق:

```powershell
.\install-node-guide.ps1
```

---

## ✨ نصيحة

بعد تثبيت Node.js، **يجب إعادة فتح PowerShell** حتى يتم تحديث PATH.

---

**رابط التحميل:** https://nodejs.org/
