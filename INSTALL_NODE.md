# 📥 دليل تثبيت Node.js

## المشكلة الحالية

`npm` غير معروف في PowerShell، مما يعني أن Node.js غير مثبت أو غير موجود في PATH.

---

## الحل: تثبيت Node.js

### الطريقة 1: التثبيت من الموقع الرسمي (موصى به) ⭐

#### الخطوة 1: تحميل Node.js

1. افتحي المتصفح واذهبي إلى: **https://nodejs.org/**
2. ستجدين زرين:
   - **LTS** (النسخة المستقرة - موصى بها) ✅
   - **Current** (النسخة الحالية)
3. اضغطي على **LTS** لتحميل النسخة المستقرة

#### الخطوة 2: تثبيت Node.js

1. شغّلي الملف الذي تم تحميله (مثل `node-v20.x.x-x64.msi`)
2. اتبعي معالج التثبيت:
   - اضغطي **Next** في جميع الخطوات
   - **⚠️ مهم جداً:** تأكدي من اختيار **"Add to PATH"** أو **"Automatically install the necessary tools"**
3. اضغطي **Install** وانتظري حتى يكتمل التثبيت
4. اضغطي **Finish**

#### الخطوة 3: التحقق من التثبيت

1. **أغلقي جميع نوافذ PowerShell** (مهم جداً!)
2. **افتحي PowerShell جديد**
3. جربي الأوامر التالية:

```powershell
node --version
# يجب أن يظهر: v20.x.x أو v18.x.x

npm --version
# يجب أن يظهر: 9.x.x أو 10.x.x
```

إذا ظهرت الأرقام، فالتثبيت نجح! ✅

---

### الطريقة 2: استخدام winget (إذا كان متوفراً)

إذا كان Windows Package Manager (winget) مثبتاً:

```powershell
# تثبيت Node.js LTS
winget install OpenJS.NodeJS.LTS

# إعادة فتح PowerShell بعد التثبيت
```

---

### الطريقة 3: استخدام Chocolatey (إذا كان مثبتاً)

إذا كان Chocolatey مثبتاً:

```powershell
# تثبيت Node.js
choco install nodejs

# إعادة فتح PowerShell بعد التثبيت
```

---

## بعد التثبيت: تشغيل التطبيق

بعد التأكد من أن Node.js و npm يعملان:

```powershell
# الانتقال إلى مجلد المشروع
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"

# تثبيت الحزم (إذا لم تكن مثبتة)
npm install

# تشغيل التطبيق
npm run dev
```

---

## حل المشاكل الشائعة

### المشكلة 1: npm لا يزال غير معروف بعد التثبيت

**الحل:**
1. تأكدي من إغلاق وإعادة فتح PowerShell
2. إذا لم يعمل، أضيفي Node.js يدوياً إلى PATH:
   - اضغطي `Win + R`
   - اكتبي: `sysdm.cpl` واضغطي Enter
   - اذهبي إلى **Advanced** → **Environment Variables**
   - في **System Variables**، ابحثي عن **Path**
   - اضغطي **Edit** → **New**
   - أضيفي: `C:\Program Files\nodejs\`
   - اضغطي **OK** في جميع النوافذ
   - أغلقي وأعيدي فتح PowerShell

### المشكلة 2: "Access Denied" أثناء التثبيت

**الحل:**
- شغّلي المثبت كمسؤول (Right-click → Run as administrator)

### المشكلة 3: التثبيت نجح لكن npm لا يعمل

**الحل:**
- أعدي تثبيت Node.js واختاري "Repair" أو "Reinstall"

---

## التحقق من التثبيت

بعد التثبيت، استخدمي السكريبت المرفق:

```powershell
.\check-and-install-node.ps1
```

أو تحققي يدوياً:

```powershell
# التحقق من Node.js
node --version

# التحقق من npm
npm --version

# التحقق من المسار
where.exe node
where.exe npm
```

---

## متطلبات النظام

- **Windows 10/11** (64-bit موصى به)
- **مساحة قرص:** ~300 MB
- **الإنترنت:** مطلوب للتحميل والتثبيت

---

## روابط مفيدة

- **الموقع الرسمي:** https://nodejs.org/
- **التوثيق:** https://nodejs.org/docs/
- **دليل npm:** https://docs.npmjs.com/

---

## الدعم

إذا استمرت المشكلة بعد التثبيت:
1. تأكدي من إعادة فتح PowerShell
2. تحققي من أن Node.js موجود في PATH
3. جربي استخدام Command Prompt بدلاً من PowerShell
4. راجعي ملف `FIX_NPM_ERROR.md` للمزيد من الحلول
