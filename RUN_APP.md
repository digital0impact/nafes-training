# 🚀 تشغيل التطبيق محلياً

## الخطوة 1: التحقق من ملف .env

قبل تشغيل التطبيق، تأكدي من وجود ملف `.env` مع القيم الصحيحة.

### إنشاء ملف .env

إذا لم يكن موجوداً، انسخي `env.example` إلى `.env`:

```powershell
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"
Copy-Item env.example .env
```

ثم افتحي ملف `.env` واملئي القيم من Supabase Dashboard.

---

## الخطوة 2: تثبيت Dependencies

```powershell
npm install
```

---

## الخطوة 3: تشغيل التطبيق

### الوضع التطويري (Development Mode)

```powershell
npm run dev
```

سيتم تشغيل التطبيق على: **http://localhost:3000**

### الوضع الإنتاجي (Production Mode)

```powershell
# البناء أولاً
npm run build

# ثم التشغيل
npm start
```

---

## الوصول إلى التطبيق

بعد تشغيل `npm run dev`، افتحي المتصفح واذهبي إلى:

**http://localhost:3000**

---

## إيقاف التطبيق

في نافذة PowerShell، اضغطي:
- `Ctrl + C` لإيقاف التطبيق

---

## حل المشاكل

### خطأ: "Port 3000 is already in use"

```powershell
# استخدمي port آخر
npm run dev -- -p 3001
```

### خطأ: "Environment variable not found"

- تأكدي من وجود ملف `.env`
- تأكدي من أن جميع المتغيرات موجودة في `.env`

### خطأ: "Database connection failed"

- تحققي من `DATABASE_URL` في `.env`
- تأكدي من أن قاعدة البيانات في Supabase تعمل
