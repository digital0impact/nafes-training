# 🔧 تثبيت Git - دليل سريع

## المشكلة الحالية

```
git : The term 'git' is not recognized
```

هذا يعني أن **Git غير مثبت** أو **غير موجود في PATH**.

---

## ✅ الحل: تثبيت Git (5 دقائق)

### الخطوة 1: تحميل Git

1. افتحي المتصفح
2. اذهبي إلى: **https://git-scm.com/download/win**
3. سيبدأ التحميل تلقائياً (أو اضغطي على **Download**)

### الخطوة 2: تثبيت Git

1. شغّلي الملف الذي تم تحميله (مثل `Git-2.x.x-64-bit.exe`)
2. اتبعي معالج التثبيت:
   - اضغطي **Next** في جميع الخطوات
   - **⚠️ مهم جداً:** في صفحة "Adjusting your PATH environment":
     - اختر **"Git from the command line and also from 3rd-party software"**
     - أو **"Use Git and optional Unix tools from the Command Prompt"**
   - باقي الإعدادات: اتركيها كما هي (افتراضي)
3. اضغطي **Install** وانتظري حتى يكتمل
4. اضغطي **Finish**

### الخطوة 3: إعداد Git (اختياري لكن موصى به)

بعد التثبيت، افتحي PowerShell جديد ونفذي:

```powershell
git config --global user.name "اسمك"
git config --global user.email "بريدك@example.com"
```

**مثال:**
```powershell
git config --global user.name "Hope"
git config --global user.email "hope@example.com"
```

### الخطوة 4: إعادة فتح PowerShell

**⚠️ مهم جداً:**
1. **أغلقي جميع نوافذ PowerShell** (مهم!)
2. **افتحي PowerShell جديد**
3. جربي:

```powershell
git --version
```

**إذا ظهر رقم الإصدار (مثل `git version 2.x.x`)، فالتثبيت نجح! ✅**

---

## 🎯 بعد التثبيت: استخدام Git

بعد التأكد من أن Git يعمل:

```powershell
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"

# التحقق من حالة Git
git status

# إضافة التغييرات
git add .

# عمل commit
git commit -m "وصف التغييرات"

# رفع التغييرات (إذا كان مربوط مع GitHub)
git push
```

---

## ❓ حل المشاكل

### المشكلة: git لا يزال غير معروف بعد التثبيت

**الحل:**
1. تأكدي من **إغلاق وإعادة فتح PowerShell**
2. إذا لم يعمل، أعدي تثبيت Git واختاري **"Git from the command line"**
3. تأكدي من اختيار **"Git from the command line and also from 3rd-party software"** أثناء إعادة التثبيت

### المشكلة: "Access Denied"

**الحل:**
- شغّلي المثبت **كمسؤول** (Right-click → Run as administrator)

---

## 📋 استخدام السكريبت

شغّلي السكريبت المرفق للتحقق:

```powershell
.\install-git-guide.ps1
```

---

## 🚀 تثبيت سريع باستخدام winget (إذا كان متوفراً)

```powershell
winget install --id Git.Git -e --source winget
```

بعد التثبيت، أغلقي وأعيدي فتح PowerShell.

---

## ✨ نصيحة

بعد تثبيت Git، **يجب إعادة فتح PowerShell** حتى يتم تحديث PATH.

---

**رابط التحميل:** https://git-scm.com/download/win
