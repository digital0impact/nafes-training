# 🔧 حل مشاكل النشر على Vercel

## خطوات التشخيص السريع

### 1. تحقق من Build Logs في Vercel

1. اذهبي إلى **Vercel Dashboard** → **Deployments**
2. اضغطي على آخر deployment (الذي فشل)
3. اضغطي على **"View Build Logs"**
4. ابحثي عن الأخطاء (عادة تظهر باللون الأحمر)

---

## المشاكل الشائعة والحلول

### ❌ خطأ: "Module not found" أو "Can't resolve"

**الأسباب المحتملة:**
- ملفات غير موجودة في المستودع
- استيرادات خاطئة
- node_modules غير موجود

**الحل:**
```powershell
# تأكدي من أن جميع الملفات موجودة
git status

# تأكدي من أن .gitignore لا يستبعد ملفات مهمة
# تأكدي من أن node_modules موجود في .gitignore (يجب أن يكون موجود)
```

**التحقق:**
- تأكدي من أن `package.json` موجود
- تأكدي من أن جميع الملفات في `src/` موجودة في Git

---

### ❌ خطأ: "Environment variable not found"

**الأسباب المحتملة:**
- متغيرات البيئة غير مضافة في Vercel
- أسماء المتغيرات غير مطابقة

**الحل:**
1. اذهبي إلى **Vercel** → **Project Settings** → **Environment Variables**
2. تأكدي من وجود جميع المتغيرات التالية:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY`
   - `DATABASE_URL`
   - `NEXTAUTH_SECRET`
   - `NEXTAUTH_URL` (للـ Production فقط)
   - `SKIP_ENV_VALIDATION`

3. **⚠️ مهم:** بعد إضافة المتغيرات، يجب إعادة النشر:
   - اذهبي إلى **Deployments**
   - اضغطي على **"Redeploy"** في آخر deployment

---

### ❌ خطأ: "Build failed" أو "Command failed"

**الأسباب المحتملة:**
- خطأ في الكود
- خطأ في TypeScript
- خطأ في Prisma

**الحل:**

#### 1. تحقق من البناء محلياً:
```powershell
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"

# تنظيف البناء السابق
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue

# البناء
npm run build
```

#### 2. إذا نجح البناء محلياً:
- تحققي من أن جميع الملفات موجودة في Git
- تأكدي من أن `.gitignore` صحيح

#### 3. إذا فشل البناء محلياً:
- أصلحي الأخطاء أولاً
- ثم حاولي النشر مرة أخرى

---

### ❌ خطأ: "Prisma Client not generated"

**الحل:**
تأكدي من أن `package.json` يحتوي على:
```json
{
  "scripts": {
    "build": "prisma generate && next build"
  }
}
```

وهذا موجود بالفعل ✅

---

### ❌ خطأ: "Database connection failed"

**الأسباب المحتملة:**
- `DATABASE_URL` غير صحيح
- كلمة المرور في `DATABASE_URL` غير صحيحة
- قاعدة البيانات غير متاحة

**الحل:**
1. تحققي من `DATABASE_URL` في Vercel:
   - يجب أن يبدأ بـ `postgresql://`
   - يجب أن يحتوي على كلمة المرور الصحيحة
   - يجب أن يكون في الصيغة: `postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres`

2. تحققي من Supabase:
   - اذهبي إلى **Supabase Dashboard** → **Settings** → **Database**
   - تأكدي من أن قاعدة البيانات تعمل
   - انسخي **Connection string** مرة أخرى

3. **⚠️ مهم:** إذا كانت كلمة المرور تحتوي على أحرف خاصة، يجب escape:
   - `@` → `%40`
   - `#` → `%23`
   - `$` → `%24`
   - `%` → `%25`
   - `&` → `%26`
   - `+` → `%2B`
   - `=` → `%3D`
   - `?` → `%3F`
   - `/` → `%2F`
   - `:` → `%3A`

---

### ❌ خطأ: "Type error" أو "TypeScript error"

**الحل:**
1. تحققي من الأخطاء في Build Logs
2. أصلحي الأخطاء في الكود
3. تأكدي من أن `tsconfig.json` صحيح

**التحقق محلياً:**
```powershell
npm run build
```

---

### ❌ خطأ: "Out of memory" أو "Build timeout"

**الحل:**
1. تحققي من حجم المشروع
2. تأكدي من أن `node_modules` غير موجود في Git
3. تأكدي من أن `.next` غير موجود في Git

**التحقق من .gitignore:**
```powershell
# يجب أن يحتوي على:
node_modules
.next
.env
```

---

### ❌ خطأ: "Invalid framework" أو "Framework not detected"

**الحل:**
1. تأكدي من أن `package.json` موجود
2. تأكدي من أن `next` موجود في `dependencies`
3. في Vercel، تأكدي من أن **Framework Preset** هو **Next.js**

---

## خطوات التحقق الشاملة

### 1. التحقق من الملفات الأساسية

```powershell
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"

# تأكدي من وجود الملفات التالية:
Test-Path "package.json"      # يجب أن يكون True
Test-Path "next.config.mjs"  # يجب أن يكون True
Test-Path "tsconfig.json"    # يجب أن يكون True
Test-Path "vercel.json"       # يجب أن يكون True
```

### 2. التحقق من Git

```powershell
# تأكدي من أن جميع الملفات موجودة في Git
git status

# تأكدي من أن .gitignore صحيح
Get-Content .gitignore
```

### 3. التحقق من البناء محلياً

```powershell
# تنظيف
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue

# البناء
npm run build
```

### 4. التحقق من متغيرات البيئة

في Vercel → Project Settings → Environment Variables:
- [ ] `NEXT_PUBLIC_SUPABASE_URL`
- [ ] `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY`
- [ ] `DATABASE_URL`
- [ ] `NEXTAUTH_SECRET`
- [ ] `NEXTAUTH_URL` (Production فقط)
- [ ] `SKIP_ENV_VALIDATION`

---

## كيفية الحصول على معلومات الخطأ

### من Vercel Dashboard:

1. **Build Logs:**
   - Vercel → Deployments → آخر deployment → View Build Logs
   - ابحثي عن الأخطاء (عادة تظهر باللون الأحمر)

2. **Function Logs:**
   - Vercel → Deployments → آخر deployment → View Function Logs
   - ابحثي عن أخطاء وقت التشغيل

3. **Deployment Details:**
   - Vercel → Deployments → آخر deployment
   - تحققي من **"Build"** و **"Deploy"** status

---

## نصائح مهمة

1. **دائماً تحققي من Build Logs** - تحتوي على معلومات مفصلة عن الخطأ
2. **تأكدي من أن البناء يعمل محلياً** قبل النشر
3. **تأكدي من أن جميع الملفات موجودة في Git**
4. **تأكدي من أن متغيرات البيئة صحيحة**
5. **بعد إضافة متغيرات البيئة، يجب إعادة النشر**

---

## إذا لم تحل المشكلة

1. انسخي رسالة الخطأ الكاملة من Build Logs
2. تحققي من:
   - هل البناء يعمل محلياً؟
   - هل جميع الملفات موجودة في Git؟
   - هل متغيرات البيئة صحيحة؟
3. ابحثي عن الخطأ في Google أو Stack Overflow

---

## روابط مفيدة

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Vercel Build Logs](https://vercel.com/docs/concepts/builds/build-logs)
