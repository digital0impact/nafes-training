# 🔧 إصلاح مشكلة DATABASE_URL

## المشكلة
`DATABASE_URL` مفقود أو غير صحيح في ملف `.env`

## الحل السريع

### الطريقة 1: استخدام السكريبت (الأسهل)

```bash
cd nafes-training
npm run fix-env
```

ثم افتحي ملف `.env` وعدّلي `DATABASE_URL`

### الطريقة 2: إنشاء الملف يدوياً

1. انسخي `env.example` إلى `.env`:
   ```powershell
   Copy-Item env.example .env
   ```

2. افتحي ملف `.env` في محرر النصوص

3. ابحثي عن السطر:
   ```
   DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"
   ```

4. استبدلي `[YOUR-PASSWORD]` و `[PROJECT-REF]` بالقيم الصحيحة من Supabase

## كيفية الحصول على DATABASE_URL من Supabase

### الخطوة 1: اذهبي إلى Supabase Dashboard
- افتحي [Supabase Dashboard](https://app.supabase.com)
- اختاري مشروعك

### الخطوة 2: احصلي على Connection String

**الطريقة الأسهل (Connection Pooling):**
1. اذهبي إلى **Settings** > **Database**
2. ابحثي عن **Connection Pooling**
3. انسخي **Connection string** > **URI**
4. الصقيها في ملف `.env` كقيمة لـ `DATABASE_URL`

**مثال:**
```
DATABASE_URL="postgresql://postgres.abcdefghijklmnop:MyPassword123@aws-0-us-east-1.pooler.supabase.com:6543/postgres"
```

**أو الاتصال المباشر:**
1. اذهبي إلى **Settings** > **Database**
2. ابحثي عن **Connection string**
3. انسخي **URI**
4. استبدلي `[YOUR-PASSWORD]` بكلمة مرور قاعدة البيانات

**مثال:**
```
DATABASE_URL="postgresql://postgres:MyPassword123@db.abcdefghijklmnop.supabase.co:5432/postgres"
```

### ⚠️ مهم: إذا كانت كلمة المرور تحتوي على أحرف خاصة

إذا كانت كلمة المرور تحتوي على: `@ # $ % & + = ? / :`

يجب تحويلها:
- `@` → `%40`
- `#` → `%23`
- `$` → `%24`
- `%` → `%25`
- `&` → `%26`
- `+` → `%2B`
- `=` → `%3D`
- `?` → `%3F`
- `/` → `%2F`
- `:` → `%3A`

**مثال:**
إذا كانت كلمة المرور: `MyP@ssw0rd#123`
يجب أن تصبح: `MyP%40ssw0rd%23123`

## التحقق من الإعدادات

بعد إضافة `DATABASE_URL`:

1. أعدي تشغيل التطبيق:
   ```bash
   npm run dev
   ```

2. اذهبي إلى صفحة الفحص:
   ```
   http://localhost:3000/debug-auth
   ```

3. اضغطي "فحص الإعدادات" - يجب أن تظهر جميع المتغيرات بـ ✓

## ملاحظات

- تأكدي من أن `DATABASE_URL` محاط بعلامات اقتباس `"`
- لا تضعي مسافات قبل أو بعد `=`
- بعد تعديل `.env`، يجب إعادة تشغيل التطبيق
