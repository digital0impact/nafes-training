# 🚀 دليل البدء من الصفر - ربط المشروع مع Vercel

## الخطوة 1: التحقق من جاهزية المشروع ✅

### 1.1 التأكد من أن البناء يعمل محلياً

افتحي PowerShell في مجلد المشروع وقومي بتنفيذ:

```powershell
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"

# تنظيف البناء السابق
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue

# البناء
npm run build
```

**إذا نجح البناء:** ✅ جاهز للمتابعة  
**إذا فشل البناء:** ⚠️ يجب إصلاح الأخطاء أولاً

---

## الخطوة 2: رفع المشروع على GitHub 📦

### 2.1 إنشاء مستودع جديد على GitHub

1. اذهبي إلى [github.com](https://github.com)
2. اضغطي على **"+"** في الزاوية العلوية اليمنى
3. اختر **"New repository"**
4. أدخلي:
   - **Repository name:** `nafes-training` (أو أي اسم تفضلينه)
   - **Description:** (اختياري) "Nafes Training Application"
   - **Visibility:** اختر **Private** أو **Public**
   - **⚠️ مهم:** لا تختاري "Add a README file" أو "Initialize with .gitignore"
5. اضغطي **"Create repository"**

### 2.2 رفع المشروع إلى GitHub

بعد إنشاء المستودع، ستظهر لك تعليمات. استخدمي هذه الأوامر:

```powershell
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"

# التحقق من Git
git status

# إذا لم يكن Git مهيأ
git init

# إضافة جميع الملفات
git add .

# عمل commit أولي
git commit -m "Initial commit: Nafes Training App"

# إضافة المستودع البعيد
# ⚠️ استبدلي YOUR_USERNAME و REPO_NAME بالقيم الفعلية
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# تغيير اسم الفرع إلى main
git branch -M main

# رفع المشروع
git push -u origin main
```

**مثال:**
```powershell
git remote add origin https://github.com/hope-/nafes-training.git
```

---

## الخطوة 3: ربط المشروع مع Vercel 🔗

### 3.1 تسجيل الدخول إلى Vercel

1. اذهبي إلى [vercel.com](https://vercel.com)
2. اضغطي على **"Sign Up"** أو **"Log In"**
3. اختر **"Continue with GitHub"** (الأسهل والأسرع)
4. سجّلي الدخول باستخدام حساب GitHub

### 3.2 إضافة مشروع جديد

1. بعد تسجيل الدخول، اضغطي على **"Add New..."** أو **"New Project"**
2. ستظهر قائمة بجميع مستودعات GitHub الخاصة بك
3. ابحثي عن `nafes-training` واضغطي **"Import"**

### 3.3 إعداد المشروع

في صفحة الإعداد:

- **Project Name:** `nafes-training` (أو اتركيه كما هو)
- **Framework Preset:** يجب أن يكون **Next.js** (سيتم اكتشافه تلقائياً)
- **Root Directory:** اتركيه فارغاً (`.`)
- **Build Command:** `npm run build` ✅ (افتراضي - صحيح)
- **Output Directory:** `.next` ✅ (افتراضي - صحيح)
- **Install Command:** `npm install` ✅ (افتراضي - صحيح)

**⚠️ لا تضغطي "Deploy" بعد - يجب إضافة متغيرات البيئة أولاً**

---

## الخطوة 4: الحصول على متغيرات البيئة من Supabase 🔑

### 4.1 تسجيل الدخول إلى Supabase

1. اذهبي إلى [supabase.com](https://supabase.com)
2. سجّلي الدخول
3. افتحي مشروعك

### 4.2 الحصول على القيم

#### أ) NEXT_PUBLIC_SUPABASE_URL
1. اذهبي إلى **Settings** → **API**
2. انسخي **Project URL** (يبدأ بـ `https://` وينتهي بـ `.supabase.co`)

#### ب) NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY
1. في نفس الصفحة (Settings → API)
2. انسخي **anon public** key أو **publishable** key
3. (هو المفتاح الطويل الذي يبدأ بـ `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`)

#### ج) DATABASE_URL
1. اذهبي إلى **Settings** → **Database**
2. اضغطي على **Connection string**
3. اختر **URI**
4. انسخي الرابط (يبدأ بـ `postgresql://postgres:...`)
5. **⚠️ مهم:** استبدلي `[YOUR-PASSWORD]` بكلمة مرور قاعدة البيانات الفعلية

---

## الخطوة 5: إضافة متغيرات البيئة في Vercel 🔐

### 5.1 فتح صفحة Environment Variables

في صفحة إعداد المشروع على Vercel:

1. ابحثي عن قسم **"Environment Variables"**
2. اضغطي على **"Add"** أو **"Add New"**

### 5.2 إضافة كل متغير

أضيفي كل متغير على حدة:

#### 1. NEXT_PUBLIC_SUPABASE_URL
```
Name: NEXT_PUBLIC_SUPABASE_URL
Value: [القيمة من Supabase - Project URL]
Environment: ☑️ Production ☑️ Preview ☑️ Development
```
اضغطي **Save**

#### 2. NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY
```
Name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY
Value: [القيمة من Supabase - anon key]
Environment: ☑️ Production ☑️ Preview ☑️ Development
```
اضغطي **Save**

#### 3. DATABASE_URL
```
Name: DATABASE_URL
Value: [Connection string من Supabase]
Environment: ☑️ Production ☑️ Preview ☑️ Development
```
اضغطي **Save**

#### 4. NEXTAUTH_SECRET
أنشئي قيمة عشوائية آمنة:

**في PowerShell:**
```powershell
[Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes([System.Guid]::NewGuid().ToString() + [System.Guid]::NewGuid().ToString()))
```

انسخي النتيجة وأضيفيها:
```
Name: NEXTAUTH_SECRET
Value: [القيمة التي تم إنشاؤها]
Environment: ☑️ Production ☑️ Preview ☑️ Development
```
اضغطي **Save**

#### 5. NEXTAUTH_URL
```
Name: NEXTAUTH_URL
Value: https://nafes-training.vercel.app
Environment: ☑️ Production فقط
```
**⚠️ ملاحظة:** سيتم إنشاء الرابط الفعلي بعد النشر الأول. يمكنك تحديثه لاحقاً.

اضغطي **Save**

#### 6. SKIP_ENV_VALIDATION
```
Name: SKIP_ENV_VALIDATION
Value: true
Environment: ☑️ Production ☑️ Preview ☑️ Development
```
اضغطي **Save**

---

## الخطوة 6: النشر 🚀

### 6.1 بدء النشر

بعد إضافة جميع متغيرات البيئة:

1. تأكدي من أن جميع المتغيرات موجودة
2. اضغطي **"Deploy"** في أسفل الصفحة
3. انتظري حتى يكتمل البناء (عادة 2-5 دقائق)

### 6.2 متابعة عملية البناء

ستظهر لك صفحة تتبع البناء:
- ✅ **Building:** جاري البناء
- ✅ **Deploying:** جاري النشر
- ✅ **Ready:** جاهز!

### 6.3 الحصول على الرابط

بعد اكتمال النشر:
- ستحصلين على رابط التطبيق (مثلاً: `https://nafes-training.vercel.app`)
- اضغطي على الرابط لفتح التطبيق

---

## الخطوة 7: التحقق من النشر ✅

### 7.1 فتح التطبيق

1. افتحي الرابط الذي حصلت عليه من Vercel
2. تأكدي من أن الصفحة الرئيسية تعمل

### 7.2 التحقق من الأخطاء

إذا واجهت أي مشاكل:

1. اذهبي إلى **Vercel Dashboard** → **Deployments**
2. اضغطي على آخر deployment
3. اضغطي على **"View Function Logs"** أو **"View Build Logs"**
4. ابحثي عن الأخطاء

---

## الخطوة 8: إعداد قاعدة البيانات (Migrations) 🗄️

### 8.1 تشغيل Migrations

بعد النشر الأول، يجب تشغيل migrations على قاعدة البيانات:

**الطريقة 1: عبر Supabase Dashboard**
1. اذهبي إلى **Supabase Dashboard** → **SQL Editor**
2. افتحي ملفات migration من `prisma/migrations/`
3. نفّذي كل migration بالترتيب

**الطريقة 2: عبر Vercel CLI**
```powershell
# تثبيت Vercel CLI
npm install -g vercel

# تسجيل الدخول
vercel login

# الانتقال إلى مجلد المشروع
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"

# سحب متغيرات البيئة
vercel env pull .env.production

# تشغيل migrations
npx prisma migrate deploy
```

---

## الخطوة 9: التحديثات المستقبلية 🔄

بعد ربط المشروع مع Vercel:

1. **كل مرة تدفعين فيها تغييرات إلى GitHub:**
   - Vercel سيكتشف التغييرات تلقائياً
   - سيبدأ بناء جديد تلقائياً
   - سيتم نشر التحديثات تلقائياً

2. **لإعادة النشر يدوياً:**
   - اذهبي إلى **Vercel Dashboard** → **Deployments**
   - اضغطي على **"Redeploy"**

---

## 📋 قائمة التحقق النهائية

- [ ] المشروع يعمل محلياً (`npm run build` نجح)
- [ ] المشروع مرفوع على GitHub
- [ ] المشروع مربوط مع Vercel
- [ ] جميع متغيرات البيئة مضافة في Vercel
- [ ] النشر الأول نجح
- [ ] التطبيق يعمل على الرابط
- [ ] Migrations تم تشغيلها

---

## 🆘 حل المشاكل الشائعة

### خطأ: "Module not found"
- تأكدي من أن جميع الملفات موجودة في المستودع
- تحققي من أن `package.json` يحتوي على جميع dependencies

### خطأ: "Environment variable not found"
- تأكدي من إضافة جميع متغيرات البيئة في Vercel
- تحققي من أن الأسماء مطابقة تماماً (حساسة لحالة الأحرف)

### خطأ: "Database connection failed"
- تحققي من `DATABASE_URL` في Vercel
- تأكدي من أن قاعدة البيانات في Supabase تعمل
- تحققي من أن كلمة المرور في `DATABASE_URL` صحيحة

### خطأ: "Build failed"
- افتحي **Deployments** → **View Build Logs**
- ابحثي عن الخطأ المحدد
- تأكدي من أن `npm run build` يعمل محلياً

---

## 📚 ملفات مساعدة

- `VERCEL_DEPLOYMENT.md` - دليل مفصل
- `VERCEL_ENV_VARS.md` - قائمة بجميع متغيرات البيئة
- `setup-vercel.ps1` - سكريبت للتحقق من الجاهزية

---

## ✨ مبروك!

إذا وصلت إلى هنا وكل شيء يعمل، مبروك! 🎉

التطبيق الآن منشور على Vercel ويمكن الوصول إليه من أي مكان في العالم!
