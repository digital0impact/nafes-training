# 🔧 حل مشكلة: npm غير معروف

## المشكلة

```
npm : The term 'npm' is not recognized as the name of a cmdlet, function, script file, or operable program.
```

هذا يعني أن **Node.js** غير مثبت أو غير موجود في PATH.

---

## الحل

### الخطوة 1: التحقق من تثبيت Node.js

افتحي **PowerShell جديد** (كمسؤول) وجربي:

```powershell
node --version
npm --version
```

**إذا ظهرت الأخطاء:**
- Node.js غير مثبت → اتبعي الخطوة 2
- Node.js مثبت لكن npm لا يعمل → اتبعي الخطوة 3

---

### الخطوة 2: تثبيت Node.js

#### الطريقة 1: من الموقع الرسمي (موصى به)

1. اذهبي إلى: https://nodejs.org/
2. حمّلي النسخة **LTS** (النسخة المستقرة)
3. شغّلي المثبت واتبعي التعليمات
4. **⚠️ مهم:** تأكدي من اختيار "Add to PATH" أثناء التثبيت

#### الطريقة 2: باستخدام Chocolatey (إذا كان مثبتاً)

```powershell
choco install nodejs
```

#### الطريقة 3: باستخدام winget

```powershell
winget install OpenJS.NodeJS.LTS
```

---

### الخطوة 3: إعادة تشغيل PowerShell

بعد تثبيت Node.js:

1. **أغلقي جميع نوافذ PowerShell**
2. **افتحي PowerShell جديد**
3. جربي مرة أخرى:
   ```powershell
   node --version
   npm --version
   ```

---

### الخطوة 4: إضافة Node.js إلى PATH يدوياً (إذا لم يعمل)

إذا كان Node.js مثبتاً لكن PowerShell لا يجده:

1. ابحثي عن موقع تثبيت Node.js (عادة):
   ```
   C:\Program Files\nodejs\
   ```

2. أضيفي المسار إلى PATH:
   - اضغطي `Win + R`
   - اكتبي: `sysdm.cpl` واضغطي Enter
   - اذهبي إلى **Advanced** → **Environment Variables**
   - في **System Variables**، ابحثي عن **Path**
   - اضغطي **Edit** → **New**
   - أضيفي: `C:\Program Files\nodejs\`
   - اضغطي **OK** في جميع النوافذ

3. **أغلقي وأعيدي فتح PowerShell**

---

### الخطوة 5: التحقق من التثبيت

بعد إعادة فتح PowerShell:

```powershell
# التحقق من Node.js
node --version
# يجب أن يظهر: v20.x.x أو v18.x.x

# التحقق من npm
npm --version
# يجب أن يظهر: 9.x.x أو 10.x.x

# التحقق من المسار
where.exe node
where.exe npm
```

---

### الخطوة 6: تشغيل التطبيق

بعد التأكد من أن npm يعمل:

```powershell
cd "C:\Users\hope-\Desktop\نافس\التطبيق\nafes-training"

# تثبيت الحزم (إذا لم تكن مثبتة)
npm install

# تشغيل التطبيق
npm run dev
```

---

## حلول بديلة

### استخدام nvm (Node Version Manager)

إذا كنت تريدين إدارة عدة إصدارات من Node.js:

1. حمّلي nvm-windows من: https://github.com/coreybutler/nvm-windows/releases
2. ثبّتي nvm-windows
3. افتحي PowerShell جديد وثبّتي Node.js:
   ```powershell
   nvm install lts
   nvm use lts
   ```

---

## التحقق من المشكلة

إذا استمرت المشكلة، تحققي من:

1. **هل Node.js مثبت؟**
   - ابحثي في قائمة Start عن "Node.js"
   - أو افتحي File Explorer واذهبي إلى `C:\Program Files\nodejs\`

2. **هل المسار موجود في PATH؟**
   ```powershell
   $env:PATH -split ';' | Select-String node
   ```

3. **جربي استخدام المسار الكامل:**
   ```powershell
   & "C:\Program Files\nodejs\npm.cmd" --version
   ```

---

## ملاحظات مهمة

- ⚠️ **يجب إعادة فتح PowerShell** بعد تثبيت Node.js
- ⚠️ **يجب إعادة فتح PowerShell** بعد تعديل PATH
- ✅ استخدمي **PowerShell كمسؤول** إذا واجهت مشاكل في الصلاحيات

---

## الدعم

إذا استمرت المشكلة:
1. تحققي من أن Node.js مثبت بشكل صحيح
2. تأكدي من إعادة فتح PowerShell
3. جربي استخدام Command Prompt بدلاً من PowerShell
4. راجعي ملف `TROUBLESHOOTING.md` للمزيد من الحلول
